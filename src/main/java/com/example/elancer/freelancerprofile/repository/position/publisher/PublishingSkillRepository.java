package com.example.elancer.freelancerprofile.repository.position.publisher;

import com.example.elancer.freelancerprofile.model.position.publisher.PublishingSkill;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PublishingSkillRepository extends JpaRepository<PublishingSkill, Long> {
}
