package com.example.elancer.freelancerprofile.repository.position.planner;

import com.example.elancer.freelancerprofile.model.position.planner.PlannerField;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PlanFieldRepository extends JpaRepository<PlannerField, Long> {
}
