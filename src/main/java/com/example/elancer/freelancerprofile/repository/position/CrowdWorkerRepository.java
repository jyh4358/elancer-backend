package com.example.elancer.freelancerprofile.repository.position;

import com.example.elancer.freelancerprofile.model.position.CrowdWorker;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CrowdWorkerRepository extends JpaRepository<CrowdWorker, Long> {
}
